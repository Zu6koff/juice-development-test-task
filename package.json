{
  "name": "jucie-development-test-task",
  "version": "0.1.0",
  "homepage": ".",
  "private": true,
  "dependencies": {
    "@vkontakte/icons": "^1.208.0",
    "@vkontakte/vk-bridge": "^2.7.2",
    "@vkontakte/vkui": "^4.39.0",
    "mobx": "^6.7.0",
    "mobx-react-lite": "^3.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "HTTPS=true react-scripts start",
    "build": "GENERATE_SOURCEMAP=false react-scripts build",
    "tunnel": "vk-tunnel --insecure=1 --http-protocol=https --ws-protocol=wss --host=0.0.0.0 --port=3000",
    "deploy": "vk-miniapps-deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@vkontakte/vk-miniapps-deploy": "^0.0.26",
    "@vkontakte/vk-tunnel": "^0.1.3"
  }
}
